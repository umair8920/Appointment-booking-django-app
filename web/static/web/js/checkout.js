/**
 * Stripe Elements checkout — mounts Payment Element, creates PaymentIntent
 * via POST /api/payments/{appointment_id}/create-intent/, confirms payment.
 * Full implementation in Milestone 8 (Docs/10).
 */
(function () {
  // Placeholder: checkout page will pass data-appointment-id + publishable key.
})();
